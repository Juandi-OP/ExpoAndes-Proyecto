// lesson_player_screen.dart
// Pantalla reproductor para visualizar bloques interactivos de lecciones.

import 'package:flutter/material.dart';

class LessonPlayerScreen extends StatelessWidget {
  const LessonPlayerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Lesson Player Placeholder')),
    );
  }
}
